"""
Analysis library for the iEEG word-encoding-geometry project.

All analysis logic lives here; the notebooks in ``notebooks/`` are pure
orchestration and only call into these modules. Import as, e.g.::

    from lib import config, dataio, supersubject, rsa, reinstatement, viz
"""
